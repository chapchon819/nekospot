<% if @top_category.present? %>
    <h1>あなたにおすすめの猫スポットジャンルは<%= @top_category.name %>です。</h1>
    <% if @recommend_spot.present? %>
        <h1>そしておすすめの猫スポットは<%= @recommend_spot.name %>です</h1>
    <% else %>
        <h1>おすすめの猫スポットはありませんでした</h1>
    <% end %>
    <%= link_to 'もう一度診断する', start_diagnostics_path, class: 'btn btn-primary' %>
<% else %>
    <h1>診断結果が見つかりませんでした。</h1>
    <%= link_to '診断を再開する', start_diagnostics_path, class: 'btn btn-primary' %>
<% end %>